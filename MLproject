name: jai_tutorial

databricks_spark_job:
    python_file: "train.py"  # the file to be executed as the pyspark driver process
    parameters: []  # parameters, only allow a list of string
    python_libraries: # dependencies to be installed as databricks cluster libraries
      - mlflow==2.4.1
      - scikit-learn
      - pandas
      - dynaconf
      - numpy
